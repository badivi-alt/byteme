datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Task {
  id              String   @id @default(uuid())
  title           String
  provider        String?
  durationMinutes Int
  skillTags       String[]
  // Add other fields as needed
}