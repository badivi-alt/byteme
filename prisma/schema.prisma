datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Task {
  id              String   @id @default(uuid())
  title           String
  provider        String?
  durationMinutes Int
  skillTags       String[]
  // Add other fields as needed
}
